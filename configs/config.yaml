project_name: traffic-sign-recognition-gtsrb
num_classes: 43

data:
  image_size: 224
  batch_size: 32
  num_workers: 2

training:
  model_name: resnet18
  pretrained: true
  epochs: 10
  learning_rate: 0.0001
  weight_decay: 0.0001

paths:
  model_dir: models
  report_dir: reports
